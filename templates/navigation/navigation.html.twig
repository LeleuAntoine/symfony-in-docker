<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <div class="d-flex justify-content-start">
            <a class="navbar-brand" href="{{ path('home') }}">AllGames</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <ul class="nav navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('list_of_games') }}">Liste des Jeux</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('last_added') }}">Dernier Ajout</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('most_popular') }}">Les Plus Populaire</a>
                </li>
            </ul>
        </div>
        <ul class="nav navbar-nav d-flex justify-content-end">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin') }}">Administration</a>
                </li>
            {% endif %}
            {% if not app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('subscription') }}">Inscription</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>